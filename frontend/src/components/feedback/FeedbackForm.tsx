'use client'
export default function FeedbackForm({ videoId, userId }: { videoId?: string, userId?: string }) {
    return <div style={{ padding: '20px', background: '#eee', borderRadius: '8px', color: '#555' }}>Feedback form is under maintenance.</div>
}
